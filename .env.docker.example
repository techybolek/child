# Docker Environment Configuration
# Copy this file to .env.docker and fill in your actual values
# Run: cp .env.docker.example .env.docker

# ============================================
# QDRANT VECTOR DATABASE (REQUIRED)
# ============================================
QDRANT_API_URL=https://your-qdrant-instance.cloud.qdrant.io
QDRANT_API_KEY=your-qdrant-api-key

# ============================================
# LLM PROVIDERS (AT LEAST ONE REQUIRED)
# ============================================

# OpenAI API Key (required for embeddings, optional for chatbot)
OPENAI_API_KEY=your-openai-api-key

# GROQ API Key (optional, but recommended for faster/free LLM calls)
GROQ_API_KEY=your-groq-api-key

# ============================================
# LLM PROVIDER SELECTION
# ============================================
# Options: "groq" or "openai"
# GROQ is default and provides faster responses with free tier

LLM_PROVIDER=groq
RERANKER_PROVIDER=groq
INTENT_CLASSIFIER_PROVIDER=groq

# ============================================
# BACKEND API URL (FOR FRONTEND)
# ============================================
# This is set automatically in docker-compose.yml
# Only change if you're running services separately
# NEXT_PUBLIC_API_URL=http://backend:8000
