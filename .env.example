# Texas Child Care Solutions - Environment Configuration
# =====================================================
# Copy this file to .env and fill in your actual values:
#   cp .env.example .env
#
# Docker Compose automatically loads .env from the project root.
# For local development (non-Docker), source this file or export variables manually.

# ============================================
# REQUIRED VARIABLES
# ============================================
# These must be set for the application to function

# Qdrant Vector Database
QDRANT_API_URL=https://your-qdrant-instance.cloud.qdrant.io
QDRANT_API_KEY=your-qdrant-api-key

# OpenAI API Key (required for embeddings)
OPENAI_API_KEY=your-openai-api-key

# GROQ API Key (required if using GROQ as LLM provider - default)
GROQ_API_KEY=your-groq-api-key

# ============================================
# PROVIDER SELECTION
# ============================================
# Options: "groq" (default, faster/free tier) or "openai"

# Main LLM for response generation
LLM_PROVIDER=groq

# Reranker model provider
RERANKER_PROVIDER=groq

# Intent classifier provider
INTENT_CLASSIFIER_PROVIDER=groq

# Query reformulator provider (for conversational mode)
REFORMULATOR_PROVIDER=groq

# ============================================
# RETRIEVAL SETTINGS
# ============================================

# Retrieval mode: "dense" (default) or "hybrid" (dense + sparse with RRF fusion)
RETRIEVAL_MODE=dense

# Conversational mode: "true" (default) enables memory/reformulation, "false" for stateless
CONVERSATIONAL_MODE=true

# ============================================
# EVALUATION SETTINGS (OPTIONAL)
# ============================================
# These are only needed for running evaluation modes against external RAG systems.
# Leave blank if not using evaluation features.

# ----- OpenAI Agent Settings -----
OPENAI_VECTOR_STORE_ID=
OPENAI_AGENT_MODEL=gpt-5-nano

# ----- Vertex AI Settings -----
VERTEX_PROJECT_ID=
VERTEX_LOCATION=us-west1
VERTEX_CORPUS_NAME=
VERTEX_AGENT_MODEL=gemini-2.5-flash

# ----- AWS Bedrock KB Settings -----
# Note: AWS credentials should be configured via ~/.aws/credentials or IAM roles
BEDROCK_KB_ID=
BEDROCK_MODEL=nova-pro
AWS_REGION=us-east-1

# ----- AWS Kendra Settings -----
# Note: AWS credentials should be configured via ~/.aws/credentials or IAM roles
KENDRA_INDEX_ID=
KENDRA_REGION=us-east-1
