# Bug Reproduction Planning

Create a new backend API test to reproduce the bug. Execute it.
plan in SPECS/PLANS/*.md to resolve the `Bug` using the exact specified markdown `Plan Format`. Follow the `Instructions` to create the plan use the `Relevant Files` to focus on the right files.

## Instructions

- You're creaating a missing test that should have caught the bug. You are not fixing the bug itself.

## Relevant Files

Focus on the following files:
- `tests/test_backend_api.py` - contains the existing backend tests
