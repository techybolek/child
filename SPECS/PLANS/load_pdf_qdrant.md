Load the pdfs to the Qdrant vector DB
# Identify the metadata
# Split into chunks
# Ensure collection tro-child-1 exists
# consider using langchain
# Create all artifacts in LOAD_DB directory
# QDRANT_API_URL and QDRANT_API_KEY are already set correctly
# Use Py Pdf Loader to avoid converting a pdf to text before chunking